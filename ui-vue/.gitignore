node_modules
dist
*.local
.vite

# Produced by ../ui/scripts/build-wasm.mjs
app/lib/wasm

# Generated by the @nuxt/ui Vite plugin on dev/build. Not committed: the type
# check does not need them (Nuxt UI declares its own components, and `.ts`
# files import explicitly — see README), so committing them would only add a
# file that goes stale between builds.
auto-imports.d.ts
components.d.ts
