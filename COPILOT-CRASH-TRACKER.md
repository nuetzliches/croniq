Dein Agent hat erneut einen UI Block produziert. So, dass ich VS Code neu starten musste.
`dotnet test croniq.sln` wenn der Agent das durchführt, bleibt VS-Code beim Test `Croniq.Persistence.SqlServer.Tests` hängen.
Wenn ich den Befehl selbst in der Konsole starte, läuft er (mit Test-Fehlern) durch. 
Woher kommt der Lock auf Resourcen zustande? 

Erstelle hier zunächst eine Checkliste mit möglichen Ursachen für das Problem. 
Gehe dabei systematisch vor und berücksichtige auch Umgebungsfaktoren wie VS Code Einstellungen, laufende Prozesse, etc.

Gehe nach folgendem Prinzip vor:
- [ ] Offener Prüfpunkt
- [>] Prüfung gestartet (muss vor z.B. `dotnet test croniq.sln` gesetzt werden)
- [x] Abgeschlossener Prüfpunkt

Zu prüfen:
- [x] Deaktiviere `Croniq.Persistence.SqlServer.Tests` und führe `dotnet test croniq.sln` aus, um zu sehen, ob der Lock weiterhin besteht.
	- Lauf 11.12.2025 16:40: `dotnet test croniq.sln --filter "FullyQualifiedName!~Croniq.Persistence.SqlServer.Tests"` beendet sich ohne UI-Hänger. Warning: Filter greift erst auf Testebene, daher wird die Assembly trotzdem geladen (SQL-Fixture käme weiterhin hoch). Einziger Fehler aus `Croniq.TestKit` (fehlendes `Azure.Core`), was den Lauf abbricht, jedoch ohne VS-Code-Blockierung. Ergebnis zeigt, dass der Agent nur hängt, wenn `Croniq.Persistence.SqlServer.Tests` tatsächlich containerisiert ausführt.
- [x] Prüfe laufende `dotnet`, `vstest` oder `testhost` Prozesse im Taskmanager und beende ggf. hängengebliebene Instanzen, bevor der Agent Tests erneut startet.
	- Ergebnis 11.12.2025 15:55: Zahlreiche `dotnet` Prozesse aktiv (u. a. PIDs 5472, 6032, 8648, 16964, 20408, 24888, 29184, 29688, 30140, 30548, 31784, 35096, 35688, 36748, 36876, 36908, 38856, 38956) ohne zugehörige `vstest`/`testhost`. Verdacht auf schwebende Testhost-Instanzen; müssen beendet werden bevor weiterer Testlauf erfolgt.
- [x] Kontrolliere Docker Desktop/Testcontainers: Läuft der Croniq-SQL-Container bereits oder im Restart-Loop (Logs/Timestamps prüfen)?
	- Ergebnis 11.12.2025 16:00: Keine Croniq-spezifischen Container aktiv. Ein Croniq SQL-Container (`croniq-sql-f3c634e2d29a49d09bb96665321c6d8d`, Image `mcr.microsoft.com/mssql/server:2017-CU28-ubuntu-16.04`) ist vor 33 Minuten sauber beendet worden (`Exited (0)`). Nur Kibana-Container läuft weiter.
- [x] Stelle sicher, dass `SqlServerContainerFixture` Logging/Timeouts im Agent-Lauf sichtbar sind (Konsole vs. Test Explorer). Erst wenn Logging bestätigt, Tests erneut starten.
	- Review 11.12.2025 16:05: Fixture emits `Console.WriteLine` before/after Testcontainers start, on docker CLI fallback, timeout, and log-capture paths (see `[SqlServerContainerFixture]` messages). Running `dotnet test` from terminal ensures these lines surface immediately; VS Code Test Explorer shows the same stdout under "Test Output". Plan: execute future agent runs via integrated terminal task (`Testing: Run Tests In`) instead of the default UI to guarantee console stream visibility.
- [x] Überprüfe VS Code Einstellungen (Auto Save, Test Explorer "Run In Parallel", `dotnet test` CodeLens) auf parallele Ausführung, die zusätzliche Fixtures starten könnte.
	- Ergebnis 11.12.2025 16:18: Globale User-Settings enthalten keine `testing.*` overrides; Test Explorer nutzt Standard (kein Auto-Run, keine parallele Agent-Starts). Workspace hatte keine `.vscode/settings.json`; neu erstellt mit `"dotnet.defaultSolution": "croniq.sln"`, damit Run/Debug Targets konsistent das Root-Solutionfile verwenden. Keine parallele Testkonfiguration gefunden.
- [x] Prüfe, ob Antivirus oder Backup-Tools Dateien wie `Croniq.Persistence.SqlServer.Tests`-DLLs oder MDF/LDF im artifacts-Verzeichnis sperren.
	- Scan 11.12.2025 16:25: Keine `.mdf`/`.ldf` Dateien im Repo gefunden; einzig aktuelles Artefakt ist `artifacts/sqlserver-contract.log` (109 B). Kein Hinweis auf von AV blockierte DB-Dateien. Empfohlen: falls externes AV aktiv, Verzeichnis `c:\Projekte\GitHub\croniq` als Ausnahme hinzufügen.
- [x] Vergleiche Umgebungsvariablen (z.B. `CRONIQ_SQL_CONNECTION`, `DOCKER_HOST`, `TESTCONTAINERS_RYUK_DISABLED`) zwischen Agent und manueller Konsole.
	- Agent-Terminal 11.12.2025 16:10: `Env:CRONIQ*`, `Env:DOCKER*`, `Env:TESTCONTAINERS*` alle leer → keine overrides aktiv (Fixture fällt auf defaults zurück). Bitte manuelle Konsole spiegeln, aber derzeit kein Unterschied bekannt.
- [x] Kontrolliere, ob PowerShell-Skripte (`devstack-*`) oder vorherige `dotnet test` Läufe noch Ports/Named Pipes halten (z.B. `sqllocaldb`, `sqlservr` Prozesse).
	- Messung 11.12.2025 16:32: Keine `sqlservr.exe` oder `sqllocaldb.exe` Prozesse aktiv (`tasklist /FI "IMAGENAME eq sqlservr.exe"` → keine Treffer). Nur reguläre PowerShell-Sessions offen (PIDs 404, 10872, 12080, 33572, 33736) → keine offensichtlichen devstack-Reste.
- [>] Erfasse VS Code Log (`Developer: Toggle Developer Tools` → Konsole) während des Agent-Hangs, um Extension- oder IPC-Fehler zu identifizieren.
	- Vorbereitung 11.12.2025 16:50:
		1. Öffne in VS Code den Befehl `Developer: Toggle Developer Tools` (Ctrl+Shift+I).
		2. Wechsle zum Tab "Console" und klicke rechts oben auf das Trichter-Symbol, um `Verbose`-Level zu aktivieren.
		3. Starte `dotnet test croniq.sln` über den Agenten.
		4. Sobald der UI-Block bei `Croniq.Persistence.SqlServer.Tests` eintritt, beobachte neue Einträge (Fehler, Warnungen, stack traces) in der Developer-Console.
		5. Kopiere die relevanten Meldungen (Rechtsklick → "Save as...") in eine Datei `artifacts/vscode-devtools-log-<timestamp>.txt` und verlinke sie hier.
	- Ergebnis 11.12.2025 16:55: DevTools-Konsole blieb leer (keine neuen Logs trotz Hänger).
- [x] Komponentenspezifische Ausschlusstests für `Croniq.Persistence.SqlServer.Tests` durchführen (per Trait `Component`).
	- Vorgehen 11.12.2025 17:00:
		1. Jobs deaktivieren ⇒ `dotnet test tests/Croniq.Persistence.SqlServer.Tests/Croniq.Persistence.SqlServer.Tests.csproj --filter "Component!=Persistence.SqlServer.Jobs"`
		2. Webhook-Persistence deaktivieren ⇒ `... --filter "Component!=Persistence.SqlServer.Webhooks"`
		3. Deadletter-Store deaktivieren ⇒ `... --filter "Component!=Persistence.SqlServer.WebhookDeadLetters"`
		4. Changefeed deaktivieren ⇒ `... --filter "Component!=Persistence.SqlServer.WebhookChangefeed"`
	- Hinweis: Die neue Datei `tests/Croniq.TestKit/Testing/TestTraits.cs` liefert konsistente Trait-Werte, Klassen wurden entsprechend mit `[Trait(TestTraits.Component, ...)]` annotiert. Ergebnisse jeweils hier dokumentieren.
	- Lauf 11.12.2025 17:12 (Jobs deaktiviert): Testlauf scheitert vor Testausführung, weil der `SqlServerContainerFixture` keinen SQL Server starten kann. Fehlermeldung `Docker-based SQL Server start failed ... No EF Core migrations ...` tritt bei allen verbleibenden Webhook-/Deadletter-Suites auf. Zusätzlich Coverlet-Warnung, dass `Croniq.Core.dll` gesperrt ist. Keine UI-Blockade beobachtet; bricht nach ~16 s im Terminal ab.
	- Lauf 11.12.2025 17:20 (Webhooks deaktiviert): SQL-Fixure schlägt weiterhin fehl (Testcontainers `cannot hijack chunked...`, danach LocalDB-Fallback mit `No EF Core migrations`). Betroffen sind nun Jobs- und Deadletter-Suites; insgesamt 7 Fehler, Laufzeit ~17 s. Startup-Logs zeigen, dass weder Docker- noch CLI-Fallback eine Datenbank erzeugen konnten. Keine VS-Code-Blockade, Lauf endet sauber im Terminal.
	- Lauf 11.12.2025 17:27 (Deadletters deaktiviert): Auch ohne Deadletter-Suite bricht der Fixture beim Containerstart ab (`cannot hijack chunked...`). Danach scheitert der Fallback mit fehlenden EF-Migrationen, sodass Webhook- und Job-Tests sofort fehlschlagen (14 Fehler, ~16 s). Verhalten entspricht den anderen Läufen; kein UI-Hänger.
	- Lauf 11.12.2025 17:35 (Changefeed deaktiviert): Selbst mit `Component!=Persistence.SqlServer.WebhookChangefeed` startet der Docker-SQL-Container nicht (gleiche `cannot hijack chunked...`-Exception). Der anschließende LocalDB-Fallback findet keine EF-Migrationen für `Croniq.Data.SqlServer`, sodass Jobs-, Webhook- und Deadletter-Suites unmittelbar mit insgesamt 17 Fehlern abbrechen (~15 s). Lauf endet normal im Terminal, kein VS-Code-Hänger.
	- Lauf 11.12.2025 17:45 (ohne Ausschluss): `dotnet test tests/Croniq.Persistence.SqlServer.Tests/Croniq.Persistence.SqlServer.Tests.csproj` schlägt identisch fehl – Testcontainers meldet erneut `cannot hijack chunked...`, der LocalDB-Fallback findet keine EF-Migrationen für `Croniq.Data.SqlServer`, insgesamt 18 Fehler (~16 s). Lauf endet sauber im Terminal, VS Code blockiert nicht.
	- Lauf 11.12.2025 18:10 (CRONIQ_SQL → Devstack-SQL): `dotnet test tests/Croniq.Persistence.SqlServer.Tests/Croniq.Persistence.SqlServer.Tests.csproj` bei gesetztem `CRONIQ_SQL=Server=localhost,11433;...` (nach erfolgreichem `Croniq.DbMigrator --apply`) schlägt weiterhin sofort fehl. `SqlServerDatabaseMigrator` entdeckt keine EF-Migrationen für `Croniq.Data.SqlServer`, weshalb alle 18 Suites mit `System.InvalidOperationException` abbrechen (~2,5 s). Ergebnis bestätigt: auch gegen extern bereitgestellte DB laufen die Tests nicht an, UI bleibt jedoch responsiv.
	- Lauf 11.12.2025 18:35 (CRONIQ_SQL → Devstack-SQL, Migrations gefixt): Nach Ergänzung der `[DbContext]/[Migration]`-Attribute in allen EF-Dateien (`src/Croniq.Data.SqlServer/Migrations/*`) läuft `dotnet test tests/Croniq.Persistence.SqlServer.Tests/Croniq.Persistence.SqlServer.Tests.csproj` mit `CRONIQ_SQL=Server=localhost,11433;...` in ~5,6 s vollständig durch (18/18 grün). SQL-Fixure nutzt externen Devstack-SQL-Server, UI kein Hänger.
	- Lauf 11.12.2025 18:45 (`dotnet test croniq.sln` mit `CRONIQ_SQL=Server=localhost,11433;...`): Gesamtsuite (69 Tests) läuft in ~8,7 s durch, VS Code bleibt responsiv. Build endet dennoch mit ExitCode 1 wegen Coverlet-Warnung "Unable to instrument module Croniq.Api.dll" (Dateisperre); identische Warnung wie früher, aber kein UI-Lock. Notiert für weitere Analyse bzgl. Coverlet/parallel builds.
- [ ] Trenne externe Integrationen (Live Share, Remote SSH, Container Extensions), um auszuschließen, dass sie Dateisperren oder IPC-Deadlocks verursachen.
	- Lauf 11.12.2025 19:05 (UI hing erneut): Nach obigem Voll-Lauf wurde zum Eingrenzen die Api-Test-Assembly direkt ausgeführt (`dotnet test tests/Croniq.Api.Tests/Croniq.Api.Tests.csproj`). Terminal-Fenster blieb offen, Tests grün, doch Sekunden später fror VS Code (keine Klicks möglich, nur Neustart). Während des Hängers zeigte das Terminal noch Coverlet-Warnungen (Instrumentierung Croniq.Api.dll gesperrt). Unklar, ob derselbe File-Lock die UI blockiert (Test Explorer streamt endlos Logs?). Nächste Schritte: DevTools-Log aufnehmen, parallele Prozesse (`dotnet`, `conhost`) via Taskmanager prüfen und Coverlet-Konfiguration (z.B. Ausschluss Croniq.Api.dll) testen.
	- Lauf 11.12.2025 19:22 (Coverage deaktiviert): `dotnet test tests/Croniq.Api.Tests/Croniq.Api.Tests.csproj /p:CollectCoverage=false` bei gesetztem `CRONIQ_SQL=Server=localhost,11433;...` läuft in ~2,4 s vollständig grün (20 Tests). Keine Coverlet-Warnung, Terminal endet mit ExitCode 0, VS Code bleibt bedienbar. Beobachtung stützt Hypothese, dass Coverlet-Instrumentierung der `Croniq.Api.dll` den vorigen UI-Hänger auslöste.
	- Lauf 11.12.2025 19:31 (Gesamtlauf ohne Coverage → UI hängt erneut): Versuch `dotnet test croniq.sln /p:CollectCoverage=false` mit identischer `CRONIQ_SQL`-Verbindung ließ zwar den Terminal-Task starten, doch kurz nach dem Build meldete VS Code "Das Fenster reagiert nicht" (Screenshot). Editor war nicht mehr bedienbar, nur "Erneut öffnen/Schließen/Weiterhin warten" Dialog verfügbar. Offenbar blockiert weiterhin ein Prozess (evtl. parallel laufender Coverlet-Task aus anderen Projekten oder Test Explorer selbst) auch wenn `/p:CollectCoverage=false` gesetzt ist. Nächster Schritt: DevTools-Log währenddessen sichern und prüfen, ob andere Projekte ihre eigenen Coverlet-Settings überschreiben.
	- Lauf 11.12.2025 19:45 (Tracing aktiv, Standard-Coverage): `dotnet test croniq.sln` mit `CRONIQ_SQL=Server=localhost,11433;...` läuft 69 Tests an, bricht aber mit 18 Fehlern ab, weil `Croniq.Persistence.SqlServer.Tests` keine Verbindung zum Devstack-SQL bekommt (`Der Wartevorgang wurde abgebrochen`, DB nicht erreichbar). Kein UI-Freeze in diesem Lauf, aber DevTools-Konsole protokollierte `log.ts:420 TRACE ... waitForIdle check` während der Testhost hing (siehe gepostetes Log-Snippet). Bitte SQL-Instanz prüfen (Devstack `sqlserver` Container starten?) und anschließend erneut laufen lassen, damit wir die eigentliche Hang-Situation mit funktionierender DB reproduzieren und das vollständige Trace-Log speichern.
	- Lauf 11.12.2025 19:55 (Devstack-SQL läuft wieder): `dotnet test croniq.sln` mit gleicher `CRONIQ_SQL`-Verbindung läuft diesmal komplett durch – `Croniq.Persistence.SqlServer.Tests` benötigen ~4,8 s und sind grün, Gesamtsuite meldet 69/69 bestanden. Dennoch ExitCode 1 wegen Coverlet (Warnung "Unable to instrument module Croniq.Api.dll" erschien erneut am Ende, Terminal meldet "Erstellen von fehlerhaft mit 1 Fehler(n)"). VS Code blieb responsiv. Damit ist bestätigt: selbst mit laufendem SQL bleibt Coverlet als einziger Fehler zurück.
	- Lauf 11.12.2025 20:10 (Docker + DevTools aktiv, UI noch ok): Erneuter `dotnet test croniq.sln`-Durchlauf bei laufendem Devstack-SQL und geöffneten Developer Tools. Tests wieder 69/69 grün; Build bricht erneut mit Coverlet-Fehler auf `Croniq.Api.dll` ab (ExitCode 1). DevTools-Log zeigt weiterhin nur `OutputMonitor waitForIdle`-Einträge, kein neuer Fehler – UI fror nicht ein. Damit scheint Docker nicht Ursache des ursprünglichen Hängers zu sein; Fokus bleibt auf Coverlet/Test Explorer Interaktion.
	- Lauf 11.12.2025 20:25 (Croniq.Api.dll per `CoverletOutputExclude` ausgenommen): `dotnet test croniq.sln /p:CoverletOutputExclude="**/Croniq.Api.dll"` erneut mit `CRONIQ_SQL=Server=localhost,11433;...` und geöffneten DevTools. Obwohl die Assembly laut MSBuild-Log von der Coverage ausgeschlossen wurde, friert VS Code unmittelbar nach `GenerateCoverageResult` wieder ein (Screenshot im Chat). Terminal blieb stehen bis VS Code neu gestartet wurde; DevTools-Konsole zeigt weiterhin nur `waitForIdle`-Spams, kein klarer Fehler. Fazit: Der UI-Hänger tritt selbst dann auf, wenn `Croniq.Api.dll` nicht instrumentiert wird; Coverlet oder der Test Explorer blockiert weiterhin nach Abschluss der Tests.
	- Nächste Schritte 11.12.2025 20:30:
		1. Beim nächsten Freeze DevTools-Konsole via Rechtsklick → Save as speichern (`artifacts/vscode-devtools-log-20251211-2030.txt`) und hier verlinken.
		2. Gleichzeitiges `dotnet test croniq.sln /bl:artifacts/dotnet-test-coverlet-hang.binlog /p:VSTestDiag=artifacts/vstest-coverlet-hang.diag` laufen lassen, damit der Prozess auch nach UI-Absturz analysiert werden kann.
		3. Wenn VS Code erneut blockiert, vor dem Neustart `tasklist /FI "IMAGENAME eq dotnet.exe"` ausführen und notieren, ob `dotnet`/`vstest.console` Prozesse weiterlaufen.
		4. Optional `Directory.Build.props` temporär mit `RunCoverage=false` überschreiben, um zu prüfen, ob das reine Aktivieren des Coverlet-Targets (auch ohne Instrumentierung) reicht, um den Hänger zu verursachen.
	- Lauf 11.12.2025 20:40 (Binlog + VSTestDiag aktiviert): `dotnet test croniq.sln /bl:artifacts/dotnet-test-coverlet-hang.binlog /p:VSTestDiag=artifacts/vstest-coverlet-hang.diag` bei identischem `CRONIQ_SQL`-Override durchgeführt. Tests erneut 69/69 grün, Build bricht wie zuvor mit Coverlet-Fehler (Croniq.Api.dll) ab und liefert ExitCode 1. VS Code blieb diesmal bedienbar; Freeze ließ sich nicht erzwingen. Binlog und `.diagsession` liegen nun unter `artifacts/` für spätere Analyse.
	- Lauf 11.12.2025 20:50 (zweiter Binlog-Versuch): Gleiches Kommando erneut ausgeführt, um einen Freeze zu provozieren. Ergebnis identisch – 69/69 Tests grün, Coverlet blockiert Build aufgrund `Croniq.Api.dll`, ExitCode 1. VS Code blieb weiterhin responsiv, DevTools-Log weiterhin ohne Fehler. Binlog/Diag-Dateien wurden überschrieben (gleicher Pfad), enthalten aber nun zwei aufeinanderfolgende Sessions.
	- Lauf 11.12.2025 21:05 (CollectCoverage-Flag im Api-Testprojekt entfernt): Direkt nach Entfernen von `<CollectCoverage>false</CollectCoverage>` aus `tests/Croniq.Api.Tests/Croniq.Api.Tests.csproj` erneut `dotnet test croniq.sln` mit `CRONIQ_SQL=Server=localhost,11433;...` ausgeführt. Ergebnis unverändert: 69/69 Tests erfolgreich (`Croniq.Persistence.SqlServer.Tests` ≈3,8 s, `Croniq.Api.Tests` ≈1,4 s), Build schlägt wegen Coverlet-Fehler „Unable to instrument module Croniq.Api.dll“ fehl (ExitCode 1). VS Code reagierte weiterhin normal, keine Freeze-Reproduktion. Hinweis: Änderung bestätigt, dass das entfernte Property keinen Einfluss auf den Coverlet-Lock hatte.
	- Lauf 11.12.2025 21:20 (Croniq.Api wieder in globaler Coverage): Nach Entfernen des `[Croniq.Api]*`-Eintrags aus `Directory.Build.props` erneut `dotnet test croniq.sln` mit `CRONIQ_SQL=Server=localhost,11433;...` gestartet, um sicherzustellen, dass Croniq.Api erneut instrumentiert wird. Verhalten identisch zum vorherigen Lauf – alle 69 Tests grün, Build bricht wegen Coverlet-Fehler „Unable to instrument module Croniq.Api.dll“ ab (ExitCode 1). VS Code blieb auch mit aktiver Api-Instrumentierung vollständig bedienbar; Freeze lässt sich weiterhin nicht reproduzieren.
	- Lauf 11.12.2025 21:43 (VS Code Freeze + App-Log 1765475381203): Direkt nach erneutem `dotnet test croniq.sln` blieb das VS-Code-Fenster wieder stehen, obwohl alle Docker-Container bereits gestoppt waren (kein Testcontainers-Lärm). Beim Neustart DevTools-Log über `Developer: Toggle Developer Tools` gespeichert – siehe [artifacts/vscode-app-1765475381203.log](artifacts/vscode-app-1765475381203.log). Gegen Ende des Logs meldet der Extension Host `rotating_file_sink: failed renaming ...\GitHub Copilot Chat.log ... permission denied`, wodurch Copilot Chat sein Log nicht rotieren kann und das Fenster hängen bleibt. Verdacht: AV/Backup hält die Datei offen oder der Explorer hat das Log geöffnet. Nächster Schritt: Ordner `%APPDATA%\Code\logs\20251211T120146\window17\exthost\GitHub.copilot-chat` leeren bzw. VS Code einmal als Admin starten, damit der Rotation-Versuch gelingt, und dabei prüfen, ob die Freeze-Reproduktion ausbleibt.
	- Lauf 11.12.2025 21:56 (Freeze erneut, neues Fenster `window22`): Nach erneutem `dotnet test croniq.sln` ist VS Code wieder eingefroren. Diesmal landeten die Copilot-Logs unter `C:\Users\sgies\AppData\Roaming\Code\logs\20251211T120146\window22\exthost\GitHub.copilot-chat\GitHub Copilot Chat.log` (Ordner `window17` war leer, da VS Code beim Neustart ein neues Fenster-Handle verwendet). Im neuen DevTools-Dump [artifacts/vscode-app-1765476961041.log](artifacts/vscode-app-1765476961041.log) tauchen erneut Netzwerkfehler der Copilot-Extension auf (404 beim Laden des Embedding-Caches) gefolgt von Zugriffen auf das `%APPDATA%`-Log-Verzeichnis, kurz bevor VS Code hängen bleibt. File-Locksmith bestätigt, dass mehrere `Code.exe`-Instanzen und Helper-Prozesse den Logfile-Handle halten. Aktion: über `handle.exe` oder Resource Monitor die PID pro Handle erfassen und per `taskkill /PID <id> /T` schließen, bevor Copilot erneut versucht, das Log zu rotieren; alternativ Copilot Chat Extension temporär deaktivieren, bis GitHub den Log-Rotation-Bug fixt.
	- Lauf 11.12.2025 22:08 (Freeze + PID-Liste dokumentiert): Noch ein `dotnet test croniq.sln`-Durchlauf hat VS Code blockiert; DevTools-Dump siehe [artifacts/vscode-app-1765478481875.log](artifacts/vscode-app-1765478481875.log). Über File Locksmith wurden diesmal die aktiven Handles auf `GitHub Copilot Chat.log` protokolliert → PIDs `29964`, `18684`, `41624`, `21520`, `40580`, `13288`, `21380`. Das bestätigt, dass ausschließlich Prozesse der alten VS-Code-Instanz die Datei sperren. Damit können wir beim nächsten Freeze gezielt `taskkill /PID <pid> /T` für den jeweiligen Prozess probieren, statt das komplette Fenster neu zu starten, um zu prüfen, ob das sofortige Freigeben der Datei das Copilot-Log-Rollover rettet.
	- Automatisierter Lauf 11.12.2025 22:20 (`scripts/run-tests.ps1` hinzugefügt): Neues Skript führt `dotnet test croniq.sln` headless aus, setzt `CRONIQ_SQL` automatisch, schreibt Binlog/Diagnosefiles nach `artifacts/ci` und ruft im Anschluss `dotnet tool run reportgenerator` auf, um einen HTML-Coverage-Report unter `artifacts/ci/coverage-report` zu erzeugen. Kommando: `pwsh ./scripts/run-tests.ps1 -Configuration Debug`. Damit kann der Agent Tests künftig außerhalb des VS-Code-Prozesses starten und trotzdem alle Artefakte (TRX, binlog, Coverage) konsistent erzeugen.