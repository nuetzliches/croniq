/**
 * Handler interfaces. Implement {@link io.croniq.runner.handler.CroniqJobHandler}
 * to register a typed handler for a {@code job_key}, or pass a lambda to
 * {@code CroniqRunner.builder().addJob(...)} for ad-hoc handlers.
 */
package io.croniq.runner.handler;
