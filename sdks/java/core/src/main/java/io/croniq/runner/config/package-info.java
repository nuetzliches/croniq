/**
 * Runner configuration. Construct {@link io.croniq.runner.config.CroniqRunnerOptions}
 * via its builder; option names and defaults mirror the .NET SDK so the same
 * operator-facing documentation applies.
 */
package io.croniq.runner.config;
