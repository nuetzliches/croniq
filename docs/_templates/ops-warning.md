::: warning Fast path first
Before opening tickets, reproduce the issue locally with `npm run docs:dev` and the Docker dev stack described in [`docs/deep-dive/devstack.md`](../deep-dive/devstack.md). Capture the failing command/output and attach it to your escalation.
:::
