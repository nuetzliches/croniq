# Croniq Deep-Dive Documentation

This directory is rendered by VitePress under `/deep-dive`. To avoid duplicate sources, the canonical landing page now lives in [`index.md`](./index.md). Use that file for edits that should appear on https://cron iq docs once built; this README simply points contributors there when browsing the repo.
